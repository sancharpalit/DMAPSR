services:
  gradio:
    container_name: dmapsr-gradio
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - 7860:7860 # PORT by default is 7860
    volumes:
      - dmapsr_weights:/dmapsr/weights/
    ipc: host # shm
    stdin_open: true
    tty: true
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: all
              capabilities:
                - gpu

volumes:
  dmapsr_weights:
    name: dmapsr_weights
    driver: local
    driver_opts:
      type: none
      o: bind
      device: ./weights

networks:
  default:
    name: dmapsr_net